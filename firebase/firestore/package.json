{
  "name": "firestore",
  "scripts": {
    "test": "firebase emulators:exec --only firestore \"vitest run --no-file-parallelism\"",
    "test:watch": "firebase emulators:exec --only firestore \"vitest watch --no-file-parallelism\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.5.0",
    "vitest": "^3.1.1"
  },
  "private": true
}
